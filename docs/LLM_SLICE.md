# LLM-empowered research slice

Scope: ~2023–2026 large-model / LLM-agent work that is **in-domain** for this atlas.

## Dedup rule
If a project is already in `data/tools.jsonl` or `papers.jsonl`, **do not add a second row**.
Instead set `themes: ["llm"]` (and keep the original `closed_loop_stage`).

## View
See [`catalogs/llm.md`](../catalogs/llm.md).

## Notes
- Linux: KernelGPT / SyzGPT / StepStone / Healer already existed — tagged only.
- IoT: FirmAgent / FORGE / FirmHive added only after absence check.
- UEFI / RTOS / bare-MCU: public LLM-native artifacts still scarce; avoid stuffing general ChatGPT wrappers.
